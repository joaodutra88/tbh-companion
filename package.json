{
  "name": "tbh-companion",
  "private": true,
  "packageManager": "pnpm@10.30.3",
  "engines": { "node": ">=20 <21" },
  "scripts": {
    "build": "turbo run build",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck",
    "dev": "turbo run dev"
  },
  "devDependencies": {
    "turbo": "^2.3.0",
    "typescript": "^5.6.0"
  }
}
